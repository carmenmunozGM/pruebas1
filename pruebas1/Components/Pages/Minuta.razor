    @using System.Collections.Generic
    @using System.Linq
    @using System.Diagnostics
    @using System.Text.Json
    @using pruebas1.Components.DTOs
    @inject pruebas1.Servicios.LoginService LoginService
    @inject HttpClient Http
@inject IJSRuntime JS
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/sweetalert2@11/dist/sweetalert2.min.css">
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

@* Botón disparador *@
<div class="d-flex justify-content-end">
    <button type="button" class="action-button-slender" style="margin-right: 40px; float: right;" @onclick="AbrirModal">
        <span style="font-size: 18px; line-height: 1;">+</span>
        Registro de actividades
    </button>
</div>

@if (IsVisible)
{
    @if (MostrarAlerta)
    {
        <div class="alert-overlay">
            <div class="alert-box @(TipoAlerta)">
                <div class="alert-content">
                    <span>@MensajeAlerta</span>
                    <button type="button" class="alert-close" @onclick="() => MostrarAlerta = false">×</button>
                </div>
            </div>
        </div>
    }

    <div class="modal-overlay">
        <div class="modal-container">
            <div class="modal-header">
                <div class="header-left">
                    <span class="header-title">Minuta de Trabajo Interna</span>
                </div>
                <button type="button" class="close-modal-btn" @onclick="Cerrar">
                    &times;
                </button>
            </div>

            <div class="modal-body">
                @* Sección de Asesor y Fecha *@
                <div style="display: flex; gap: 20px; align-items: flex-start; margin-bottom: 20px;">
                    <div class="field-box" style="flex: 2;">
                        <label>Asesor (Clave y Nombre)</label>
                        <input type="text" value="@($"{ClaveAsesor} - {NombreAsesor}")" disabled class="input-readonly" />
                    </div>

                    <div class="field-box date-box" style="flex: 1;">
                        <label>Fecha</label>
                        <DatePicker Value="FechaSeleccionada"
                                    ValueChanged="OnFechaInicioChanged"
                                    CssClass="form-control custom-dp" />
                    </div>
                </div>

                @* Tabla de Actividades *@
                <div class="table-wrapper">
                    <table class="custom-table">
                        <thead>
                            <tr>
                                <th class="col-cliente">Cliente (Clave y Nombre)</th>
                                <th class="col-actividad">Actividad</th>
                                <th style="width: 110px;">Periodo</th>
                                <th style="width: 110px;">Hrs. Invertidas</th>
                                <th style="width: 90px;">Acción</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var fila in FilasMinuta)
                            {
                                <tr>
                                    <td class="col-cliente">
                                        <select @bind="fila.ClienteId" class="cell-input select-limitado">
                                            <option value="0">Seleccionar Empresa...</option>
                                            @foreach (var cliente in listaClientes)
                                            {
                                                <option value="@cliente.Id">@cliente.Clave - @cliente.RazonSocial</option>
                                            }
                                        </select>
                                    </td>
                                    <td class="col-actividad">
                                        <div class="activity-stack">
                                            <select @bind="fila.IdActividad" class="cell-input">
                                                <option value="0">Seleccione...</option>
                                                @foreach (var act in ListadoActividades)
                                                {
                                                    <option value="@act.Id">@act.Descripcion</option>
                                                }
                                            </select>
                                            @if (fila.IdActividad == 99)
                                            {
                                                <input type="text" @bind="fila.OtraActividad" class="input-subtle" placeholder="Especifique actividad..." />
                                            }
                                        </div>
                                    </td>
                                    <td><input type="text" @bind="fila.Periodo" placeholder="Ej. Ene-Feb" class="cell-input" /></td>
                                    <td style="overflow: visible !important; position: relative;">
                                        <div class="timepicker-container">
                                            <TimePickerList @bind-Value="fila.Horas" />
                                        </div>
                                    </td>
                                    <td>
                                        <div class="cell-actions">
                                            @if (fila == FilasMinuta.Last())
                                            {
                                                <button type="button" class="action-icon btn-add" @onclick="AgregarFila" title="Agregar">
                                                    +
                                                </button>
                                            }
                                            <button type="button" class="action-icon btn-del" @onclick="() => EliminarFila(fila)" title="Eliminar">
                                                🗑
                                            </button>
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>

                @* Resumen y Observaciones *@
                <div class="summary-section">
                    <div class="notes-area">
                        <label>Comentarios u Observaciones</label>
                        <textarea @bind="Observaciones" rows="2" placeholder="Notas adicionales..."></textarea>
                    </div>
                    <div class="total-box-display">
                        <div class="total-label">HORAS TOTALES</div>
                        <div class="total-number">@CalcularTotalHoras()</div>
                    </div>
                </div>
            </div>

            <div class="modal-footer">
                <button type="button" class="btn-secondary" @onclick="Cerrar">Cancelar</button>
                <button type="button" class="btn-primary" @onclick="Guardar">Guardar Minuta</button>
            </div>
        </div>
    </div>
}

<style>
    /* ================= ESTILOS BASE ================= */
    .modal-overlay {
        position: fixed;
        inset: 0;
        background: rgba(0,0,0,0.5);
        display: flex;
        justify-content: center;
        align-items: center;
        z-index: 1000;
    }

    .modal-container {
        background: #ffffff;
        width: 90%;
        max-width: 1000px;
        border-radius: 12px;
        overflow: hidden;
        display: flex;
        flex-direction: column;
        max-height: 90vh;
    }

    .modal-header {
        padding: 16px 24px;
        background: #f8fafc;
        display: flex;
        justify-content: space-between;
        align-items: center;
        border-bottom: 1px solid #e2e8f0;
    }

    .header-title {
        font-weight: 700;
        font-size: 1.2rem;
        color: #1e293b;
    }

    .close-modal-btn {
        background: none;
        border: none;
        font-size: 24px;
        cursor: pointer;
    }

    .modal-body {
        padding: 24px;
        overflow-y: auto;
    }

    .field-box {
        display: flex;
        flex-direction: column;
        gap: 4px;
    }

        .field-box label {
            font-size: 0.85rem;
            font-weight: 600;
            color: #64748b;
        }

    .input-readonly {
        background: #f1f5f9;
        border: 1px solid #e2e8f0;
        padding: 8px 12px;
        border-radius: 8px;
        width: 100%;
    }

    /* ================= TABLA ================= */
    .table-wrapper {
        border: 1px solid #e2e8f0;
        border-radius: 8px;
        /* Cambiamos hidden por visible para que el panel no se corte */
        overflow: visible !important;
        background: #ffffff;
    }

    .custom-table {
        width: 100%;
        border-collapse: collapse;
    }

        .custom-table th {
            background: #f8fafc;
            padding: 12px;
            text-align: left;
            font-size: 0.85rem;
            color: #64748b;
        }

        .custom-table td {
            padding: 8px;
            border-bottom: 1px solid #f1f5f9;
        }

    .cell-input, select, textarea {
        width: 100%;
        padding: 6px 10px !important;
        border: 1px solid #e5e7eb;
        border-radius: 6px;
        font-size: 0.9rem;
    }

    .activity-stack {
        display: flex;
        flex-direction: column;
        gap: 4px;
    }

    .input-subtle {
        border: 1px dashed #22c55e;
        background: #f0fdf4;
        color: #166534;
        padding: 4px 8px;
        border-radius: 4px;
        font-size: 0.8rem;
    }

    /* ================= BOTONES ACCIÓN ================= */
    .action-button-slender {
        background: #0284c7;
        color: white;
        border: none;
        padding: 8px 16px;
        border-radius: 20px;
        display: flex;
        align-items: center;
        gap: 8px;
        cursor: pointer;
    }

    .cell-actions {
        display: flex;
        gap: 8px;
    }

    .action-icon {
        width: 32px;
        height: 32px;
        border-radius: 6px;
        border: none;
        display: flex;
        align-items: center;
        justify-content: center;
        cursor: pointer;
    }

    .btn-add {
        background: #dcfce7;
        color: #16a34a;
    }

    .btn-del {
        background: #fee2e2;
        color: #dc2626;
    }

    /* ================= TIMEPICKER ================= */

    .timepicker-container {
        position: relative;
        width: 100%;
        /* Asegura que el contenedor no corte el panel */
        overflow: visible !important;
    }

    .date-box, .timepicker-container {
        position: relative;
    }

    .timepicker-panel {
        /* Usamos fixed si absolute sigue dando problemas,
               pero absolute con z-index millonario suele bastar */
        position: absolute !important;
        top: calc(100% + 5px) !important;
        left: 0 !important;
        /* Un z-index extremadamente alto para superar el modal */
        z-index: 99999999 !important;
        display: flex;
        gap: 6px;
        background: #ffffff;
        border-radius: 14px;
        box-shadow: 0 10px 30px rgba(0,0,0,0.3);
        padding: 8px;
        min-width: 150px;
    }

    .timepicker-column {
        scrollbar-width: none; /* Firefox */
    }

        .timepicker-column::-webkit-scrollbar {
            display: none; /* Chrome/Safari */
        }

    /* Hacemos los inputs de la tabla un poco más delgados verticalmente */
    .cell-input, select {
        padding: 6px 10px !important;
    }

    .timepicker-input {
        width: 100%;
        padding: 10px 12px;
        border-radius: 12px;
        border: 1px solid #e5e7eb;
        background: #f8fafc;
        font-size: 0.9rem;
        cursor: pointer;
    }

        .timepicker-input:focus {
            outline: none;
            border-color: #22c55e;
            box-shadow: 0 0 0 3px rgba(34,197,94,0.18);
        }

    /* PANEL */

    .timepicker-panel {
        position: absolute;
        top: calc(100% + 6px);
        left: 0;
        display: flex;
        gap: 6px;
        background: #ffffff;
        border-radius: 14px;
        box-shadow: 0 20px 45px rgba(0,0,0,0.25);
        padding: 8px;
        z-index: 99999;
    }

    /* COLUMNAS */

    .timepicker-column {
        max-height: 180px;
        overflow-y: auto;
        scrollbar-width: thin;
        padding: 4px;
    }

    /* ITEMS */

    .timepicker-item {
        padding: 6px 10px;
        border-radius: 8px;
        font-size: 0.85rem;
        text-align: center;
        cursor: pointer;
        color: #334155;
        transition: background 0.15s ease, color 0.15s ease;
    }

        .timepicker-item:hover {
            background: #f0fdf4;
        }

        .timepicker-item.selected {
            background: #22c55e;
            color: #ecfdf5;
            font-weight: 700;
        }

    /* SCROLL BONITO */

    .timepicker-column::-webkit-scrollbar {
        width: 6px;
    }

    .timepicker-column::-webkit-scrollbar-thumb {
        background: #cbd5e1;
        border-radius: 10px;
    }
    /* ================= SUMMARY SECTION ================= */
    .summary-section {
        display: grid;
        grid-template-columns: 2fr 1fr;
        gap: 20px;
        margin-top: 20px;
        align-items: start;
    }

    .total-box-display {
        background: #f8fafc;
        border: 1px solid #e2e8f0;
        border-radius: 12px;
        padding: 16px;
        text-align: center;
    }

    .total-label {
        font-size: 0.75rem;
        font-weight: 800;
        color: #64748b;
        letter-spacing: 0.05em;
    }

    .total-number {
        font-size: 2rem;
        font-weight: 800;
        color: #1e293b;
    }

    /* ================= ALERTAS ================= */
    .alert-overlay {
        position: fixed;
        inset: 0;
        z-index: 99999;
        display: flex;
        justify-content: center;
        align-items: center;
        pointer-events: none;
    }

    .alert-box {
        min-width: 350px;
        padding: 16px 20px;
        border-radius: 8px;
        box-shadow: 0 10px 30px rgba(0,0,0,.25);
        pointer-events: all;
    }

        .alert-box.error {
            background-color: #ffe5e5;
            border-left: 5px solid #d32f2f;
            color: #b71c1c;
        }

        .alert-box.success {
            background-color: #e6f4ea;
            border-left: 5px solid #2e7d32;
            color: #1b5e20;
        }

    .alert-content {
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .alert-close {
        background: none;
        border: none;
        font-size: 20px;
        cursor: pointer;
    }

    /* ================= MODO OSCURO ================= */
    [data-theme="dark"] .action-button-slender {
        background-color: #03324a;
        color: #ffffff;
    }

    [data-theme="dark"] .modal-container {
        background: #0f172a !important;
        color: #f1f5f9;
    }

    [data-theme="dark"] .modal-header {
        background: #1e293b;
        border-bottom: 1px solid #334155;
    }

    [data-theme="dark"] .header-title {
        color: #f1f5f9;
    }

    [data-theme="dark"] .input-readonly {
        background: #1e293b !important;
        border: 1px solid #334155 !important;
        color: #94a3b8 !important;
    }

    [data-theme="dark"] .table-wrapper {
        background: #1e293b !important;
    }

    [data-theme="dark"] .custom-table thead {
        background: #0f172a !important;
    }

    [data-theme="dark"] .custom-table th {
        border-bottom: 2px solid #334155 !important;
    }

    [data-theme="dark"] .custom-table td {
        border-bottom: 1px solid #334155 !important;
    }

    [data-theme="dark"] .cell-input, [data-theme="dark"] select, [data-theme="dark"] textarea, [data-theme="dark"] .timepicker-input {
        background: #0f172a !important;
        border: 1px solid #334155 !important;
        color: #f1f5f9 !important;
    }

    [data-theme="dark"] .input-subtle {
        background: rgba(34, 197, 94, 0.1) !important;
        color: #4ade80 !important;
    }

    [data-theme="dark"] .btn-del {
        background: rgba(185, 28, 28, 0.2) !important;
        color: #ef4444 !important;
    }

    [data-theme="dark"] .btn-add {
        background: rgba(21, 128, 61, 0.2) !important;
        color: #4ade80 !important;
    }

    [data-theme="dark"] .total-box-display {
        background: #1e293b !important;
        border: 1px solid #334155 !important;
    }

    [data-theme="dark"] .total-number {
        color: white !important;
    }

    [data-theme="dark"] .timepicker-panel {
        background: #1e293b !important;
        border: 1px solid #334155 !important;
    }

    [data-theme="dark"] .timepicker-item {
        color: #cbd5e1 !important;
    }

        [data-theme="dark"] .timepicker-item:hover {
            background: #334155 !important;
        }

    .modal-footer {
        padding: 16px 24px;
        display: flex;
        justify-content: flex-end;
        gap: 12px;
        background: #f8fafc;
        border-top: 1px solid #e2e8f0;
    }

    .btn-secondary, .btn-primary {
        padding: 10px 20px;
        border-radius: 10px;
        font-weight: 600;
        cursor: pointer;
        border: none;
        transition: all 0.2s ease;
    }

    /* Botón Cancelar */
    .btn-secondary {
        background: #e2e8f0;
        color: #475569;
    }

        .btn-secondary:hover {
            background: #cbd5e1;
        }

    /* Botón Guardar */
    .btn-primary {
        background: #22c55e; /* Verde vibrante */
        color: white;
        box-shadow: 0 4px 12px rgba(34, 197, 94, 0.2);
    }

        .btn-primary:hover {
            background: #16a34a;
            box-shadow: 0 6px 15px rgba(34, 197, 94, 0.3);
        }

    /* ================= ESTILOS MODO OSCURO (BOTONES) ================= */
    [data-theme="dark"] .modal-footer {
        background: #0f172a !important;
        border-top: 1px solid #334155 !important;
    }

    [data-theme="dark"] .btn-secondary {
        background: #334155 !important;
        color: #f1f5f9 !important;
    }

    [data-theme="dark"] .btn-primary {
        background: #22c55e !important; /* Mantenemos el verde para el éxito */
    }

    /* Los botones de la tabla en modo oscuro */
    [data-theme="dark"] .btn-add {
        background: rgba(34, 197, 94, 0.2) !important;
        color: #4ade80 !important;
    }

    [data-theme="dark"] .btn-del {
        background: rgba(239, 68, 68, 0.2) !important;
        color: #f87171 !important;
    }
</style>
    @code {
        [Parameter] public EventCallback OnSave { get; set; }
        private List<ClientesDTO> listaClientes = new();
        private bool IsVisible = false;

        private string NombreAsesor = "";
        private int ClaveAsesor = 0;
        private DateTime FechaSeleccionada = DateTime.Now;
        private string Observaciones = "";
        private List<ActividadMinutaDTO> ListadoActividades = new();

    private bool MostrarAlerta = false;
    private string MensajeAlerta = "";
    private string TipoAlerta = "error"; 

        public class FilaMinuta
        {
            public int ClienteId { get; set; }
            public int IdActividad { get; set; }
            public string? OtraActividad { get; set; }   
            public string Periodo { get; set; } = "";
            public TimeOnly? Horas { get; set; }
        }


        private void OnFechaInicioChanged(DateTime? nuevaFecha)
        {
            FechaSeleccionada = nuevaFecha ?? DateTime.Now;
        }
        private List<FilaMinuta> FilasMinuta = new();

        private async Task AbrirModal()
        {
            var usuario = LoginService.obtenerUsuarioLogueado();

            NombreAsesor = !string.IsNullOrWhiteSpace(usuario.NombreRed)
                ? usuario.NombreRed
                : usuario.NombreUsuario;

            ClaveAsesor = usuario.IdEmpleado;

            FilasMinuta = new List<FilaMinuta> { new FilaMinuta() };
            IsVisible = true;

            try
            {
                // =============================
                // 1️⃣ Cargar clientes
                // =============================
                var respClientes = await Http.GetAsync("/api/servicio-cliente/admin/clientes");

                if (respClientes.IsSuccessStatusCode)
                {
                    listaClientes = await respClientes.Content.ReadFromJsonAsync<List<ClientesDTO>>() ?? new();
                }
                else
                {
                    Debug.WriteLine("Error cargando clientes: " + respClientes.StatusCode);
                }

                // =============================
                // 2️⃣ Cargar empleado
                // =============================
                var respEmpleado = await Http.GetAsync($"/empleado/Obtener/{usuario.IdUsuario}");
                Debug.WriteLine("La clave del empleado es " + usuario.IdUsuario);
                if (!respEmpleado.IsSuccessStatusCode)
                {
                    Debug.WriteLine("Error obteniendo empleado: " + usuario.IdUsuario  + respEmpleado.StatusCode);
                    return;
                }

            var empleado = await respEmpleado.Content.ReadFromJsonAsync<EmpleadoDTO>();

            if (empleado == null || string.IsNullOrWhiteSpace(empleado.Area))
            {
                Debug.WriteLine("Empleado sin área.");
                return;
            }

            // =============================
            // 3️⃣ Cargar actividades por área
            // =============================
            var respActividades = await Http.GetAsync(
                $"/Minutas/catalogo?area={Uri.EscapeDataString(empleado.Area)}"
            );

            Debug.WriteLine(Uri.EscapeDataString(empleado.Area) + " " + respActividades);
       
                if (respActividades.IsSuccessStatusCode)
                {
                    ListadoActividades = await respActividades
                        .Content
                        .ReadFromJsonAsync<List<ActividadMinutaDTO>>() ?? new();

                    Debug.WriteLine("Cantidad actividades: " + ListadoActividades.Count);
                }

            else
            {
                Debug.WriteLine("Error cargando actividades: " + respActividades.StatusCode);
            }
        }
        catch (Exception ex)
        {
            Debug.WriteLine("ERROR GENERAL: " + ex.Message);
        }

        StateHasChanged();
    }


        private void Cerrar() => IsVisible = false;

        private void AgregarFila() => FilasMinuta.Add(new FilaMinuta());

        private void EliminarFila(FilaMinuta fila)
        {
            if (FilasMinuta.Count > 1) FilasMinuta.Remove(fila);
        }

        private string CalcularTotalHoras()
        {
            TimeSpan total = TimeSpan.Zero;
            foreach (var f in FilasMinuta)
            {
                if (f.Horas.HasValue)
                {
                    total = total.Add(new TimeSpan(f.Horas.Value.Hour, f.Horas.Value.Minute, 0));
                }
            }
            return $"{(int)total.TotalHours} Horas, {total.Minutes} Minutos";
        }

        private bool Guardando = false;
    private string? ErrorGuardar;

    private async Task Guardar()
    {
        string mensaje = "";

        // Validar fecha
        if (FechaSeleccionada == default)
            mensaje += "• Debe seleccionar la fecha.\n";

        // Validar filas
        if (FilasMinuta == null || !FilasMinuta.Any())
            mensaje += "• Debe agregar al menos una actividad.\n";
        else
        {
            foreach (var fila in FilasMinuta)
            {
                if (fila.ClienteId <= 0)
                    mensaje += "• Debe seleccionar el cliente.\n";

                if (fila.IdActividad <= 0)
                    mensaje += "• Debe seleccionar la actividad.\n";

                if (string.IsNullOrWhiteSpace(fila.Periodo))
                    mensaje += "• Debe capturar el periodo.\n";

                if (!fila.Horas.HasValue)
                    mensaje += "• Debe capturar las horas invertidas.\n";
            }
        }

        // Si hay errores → mostrar alerta tipo SweetAlert
        if (!string.IsNullOrEmpty(mensaje))
        {
            await JS.InvokeVoidAsync("Swal.fire", new
            {
                icon = "error",
                title = "Campos obligatorios incompletos",
                text = mensaje
            });
            return;
        }

        // Construir DTO
        var dto = new
        {
            Fecha = FechaSeleccionada,
            Comentarios = Observaciones,
            Actividades = FilasMinuta.Select(f => new
            {
                ClaveCliente = f.ClienteId,
                IdActividad = f.IdActividad,
                Periodo = f.Periodo,
                HorasInvertidas = f.Horas!.Value.Hour * 60 + f.Horas.Value.Minute
            }).ToList()
        };

        try
        {
            var response = await Http.PostAsJsonAsync("/Minutas/crear", dto);

            if (response.IsSuccessStatusCode)
            {
                await JS.InvokeVoidAsync("Swal.fire", new
                {
                    icon = "success",
                    title = "Minuta guardada con éxito 🎉",
                    confirmButtonText = "OK"
                });

                Cerrar();
            }
            else
            {
                await JS.InvokeVoidAsync("Swal.fire", new
                {
                    icon = "error",
                    title = "Error al guardar",
                    text = "Ocurrió un error al intentar guardar la minuta."
                });
            }
        }
        catch (Exception ex)
        {
            await JS.InvokeVoidAsync("Swal.fire", new
            {
                icon = "error",
                title = "Error inesperado",
                text = ex.Message
            });
        }
    }



    private void LanzarAlerta(string mensaje, string tipo)
    {
        MensajeAlerta = mensaje;
        TipoAlerta = tipo;
        MostrarAlerta = true;
        StateHasChanged();
    }

    }
